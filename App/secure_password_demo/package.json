{
    "name": "secure-password-demo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "npm run build --workspace=client && rm -rf dist && cp -r client/dist ."
    }
}